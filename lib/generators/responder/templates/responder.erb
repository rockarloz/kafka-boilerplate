# frozen_string_literal: true

# A generic karafka responder
class <%= class_name %> < ApplicationResponder
  topic :<%= topic %>
  def respond(event)
    respond_to :<%= topic %>, event.data.to_json, headers: event.headers
  end
end
